# Secret patterns to block from commits
# Each line is a regex pattern that will be checked against staged files

# Database passwords (long random strings after DB_PASSWORD= or password:)
DB_PASSWORD=[A-Za-z0-9+/=]{20,}
password.*['"]\s*[:=]\s*['"][A-Za-z0-9+/=]{20,}['"]

# AWS keys
AKIA[0-9A-Z]{16}
aws_secret_access_key\s*=

# Private keys
-----BEGIN (RSA |EC |DSA )?PRIVATE KEY-----

# Generic API keys / tokens (long hex or base64 strings assigned to variables)
(api_key|apikey|api_secret|secret_key|access_token)\s*[:=]\s*['"][A-Za-z0-9+/=]{16,}['"]
